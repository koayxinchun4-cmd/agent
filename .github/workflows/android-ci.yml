name: Android CI

# Primary automatic CI gate while reliability errors are being reduced.
# Other workflows are parked/manual-only until Android CI stability improves.
on:
  push:
    branches: [ "main" ]
    paths:
      - "app/**"
      - "gradle/**"
      - "gradlew"
      - "gradle.properties"
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - ".github/workflows/android-ci.yml"
  pull_request:
    branches: [ "main" ]
    paths:
      - "app/**"
      - "gradle/**"
      - "gradlew"
      - "gradle.properties"
      - "settings.gradle.kts"
      - "build.gradle.kts"
      - ".github/workflows/android-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: android-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Build, test, and lint
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify checked out commit and reliability test source
        run: |
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          test -f app/src/test/java/com/example/agent/nexus/agent/NexusAgentReliabilityTest.kt
          ! grep -R "error.message" app/src/test/java/com/example/agent/nexus/agent/NexusAgentReliabilityTest.kt
          git status --short
          git rev-parse HEAD

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make Gradle Wrapper executable
        run: chmod +x ./gradlew

      - name: Create local environment file
        run: printf 'GEMINI_API_KEY=%s\n' "${{ secrets.GEMINI_API_KEY }}" > .env

      - name: Run unit tests
        run: ./gradlew clean :app:testDebugUnitTest --stacktrace --no-daemon

      - name: Run Android lint
        run: ./gradlew :app:lintDebug --stacktrace --no-daemon

      # APK packaging is intentionally disabled during the roadmap-all run.
      # Build the APK only after all roadmap phases are green.
      # - name: Build Debug APK
      #   run: ./gradlew :app:assembleDebug --stacktrace --no-daemon

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-test-reports
          path: |
            app/build/test-results/**/*.xml
            app/build/reports/tests/**
          if-no-files-found: ignore

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-lint-report
          path: |
            app/build/reports/lint-results-debug.*
            app/build/reports/lint-results-debug/
          if-no-files-found: ignore

  verify-wrapper:
    name: Verify Gradle wrapper
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

# APK metadata/package validation is intentionally disabled for roadmap-all.
# - job: package-check
#   name: Check packaged APK metadata
#   needs: verify
#   runs-on: ubuntu-latest
#   steps:
#     - name: Download Debug APK
#       uses: actions/download-artifact@v4
#       with:
#         name: app-debug
#         path: artifacts
#     - name: Check APK exists
#       run: test -s artifacts/app-debug.apk
